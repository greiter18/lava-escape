export class InputHandler {

  constructor(){
    document.addEventListener('keydown', event => {
      switch(event.key){
      case ArrowRight:
        break

      case ArrowLeft:
        break

      case Space:
      break
      }
    })
  }
}